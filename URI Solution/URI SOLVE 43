#include<stdio.h>

int main()

{
    double a,b,c,d,e;

    scanf("%lf %lf %lf",&a,&b,&c);

    if(a<b+c && b<a+c && c<a+b)
    {
        e=a+b+c;
        printf("Perimetro = %0.1lf\n",e);
    }
    else
    {
        d=0.5*(a+b)*c;
        printf("Area = %0.1lf\n",d);
    }
}
